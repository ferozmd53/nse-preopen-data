name: Test Schedule

on:
  schedule:
    - cron: '*/5 * * * *'   # Every 5 minutes
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print hello
        run: echo "Hello Feroz! Scheduled run at $(date)"
