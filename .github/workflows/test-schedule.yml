name: Simple Test

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:
  push:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Print Time
        run: echo "Hello! Current time is $(date)"
